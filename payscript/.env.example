# Django secret key (use `get_random_secret_key()` to generate one)
SECRET_KEY=your-secret-key-here

# Set to True or False (remember: no quotes)
DEBUG=True

# PostgreSQL database configuration
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
